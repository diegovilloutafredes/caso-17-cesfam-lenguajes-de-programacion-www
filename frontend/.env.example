# Copiar a .env.local (dev) o configurar en Vercel (build) y NO versionar el real.
# URL base del backend (el ApiGateway/BFF). En local :8000; en cloud, el dominio de Railway.
VITE_API_URL=http://localhost:8000
